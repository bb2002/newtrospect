import type { AnalysisKind } from "@newtrospect/core/server";

/**
 * 모델에게 직접 코드포인트 인덱스를 출력하라고 시키지 않는다.
 * LLM 은 인덱스 셈을 거의 항상 틀린다 — 대신 *원문의 정확한 부분 문자열*을
 * 출력하게 하고, 서버에서 본문 내 매칭으로 좌표를 도출한다 (matchSpans 참고).
 *
 * 2026-05-21 S2 spike 후 보강:
 * - quantitative 의 detection 카테고리를 명시해 recall 끌어올림 (8b 기준 80% → 목표 90%+)
 * - JSON 외 출력 금지를 더 단호하게
 * - one-shot 예시 추가 — 모델이 schema 를 더 정확히 따르도록
 */

interface PromptShape {
  system: string;
  /** 모델의 JSON 출력에서 각 span 객체가 가져야 할 키들. */
  itemKeys: readonly string[];
}

const COMMON_TAIL = [
  "출력은 **JSON 객체 하나**여야 한다. 마크다운 코드펜스(```), 설명, 인사말, 어떠한 부가 텍스트도 절대 출력하지 말 것.",
  "본문에 *정확히 그대로 존재하지 않는* 표현은 절대 출력하지 말 것 — 매칭 실패로 버려진다.",
].join("\n");

export const PROMPTS: Record<AnalysisKind, PromptShape> = {
  term: {
    system: [
      "당신은 한국 뉴스 텍스트에서 *일반 독자에게 어려울 수 있는 전문 용어·약어·외래어·법률 용어·기술 용어*를 찾아 문맥 기반으로 짧게 설명하는 도우미다.",
      "아래 본문에서 그런 단어/표현을 최대 12개까지 골라, 각 항목마다 본문 *정확히 그대로의 부분 문자열* 과 한 문장(60자 이내) 의 설명을 출력한다.",
      '출력 형식 예: {"items":[{"text":"GDP","explanation":"한 나라가 일정 기간 생산한 모든 재화·서비스의 시장 가치"},{"text":"양자화","explanation":"AI 모델의 가중치를 더 작은 비트수로 압축하는 기법"}]}',
      COMMON_TAIL,
    ].join("\n"),
    itemKeys: ["text", "explanation"],
  },
  sensational: {
    system: [
      "당신은 한국 뉴스 텍스트에서 *자극적·선동적·감정 유도적* 표현을 찾는 검수자다.",
      "예: 충격, 경악, 초유의, 발칵, 분노, 폭주, 격분, 공포, 폭탄, 마비, 가공할, 사상 최악 같은 정서적 과장·강한 어휘.",
      "아래 본문에서 그런 표현을 최대 15개까지 골라, 본문 *정확히 그대로의 부분 문자열* 과 왜 자극적인지 한 문장(50자 이내) 이유를 출력한다.",
      '출력 형식 예: {"items":[{"text":"충격적","reason":"독자 감정을 직접 자극하는 강한 형용사"},{"text":"발칵 뒤집혔","reason":"사실보다 분위기 묘사로 과장"}]}',
      COMMON_TAIL,
    ].join("\n"),
    itemKeys: ["text", "reason"],
  },
  quantitative: {
    system: [
      "당신은 한국 뉴스 본문에서 *수치·통계·정량 표현*을 모두 찾아내는 추출기다.",
      "다음 카테고리를 빠짐없이 포착하라:",
      "  - 퍼센트(예: 12.4%, 60% 이상)",
      "  - 금액(예: 1820조 원, 1조 2천억 원, 50달러)",
      "  - 수량·횟수(예: 30여 곳, 5회, 200K)",
      "  - 비율·배수(예: 2배, GDP 대비)",
      "  - 기간·시점(예: 3주 연속, 5년 만의, 4분기)",
      "  - 증감(예: 12.4% 증가, 8.7% 떨어, 41% 늘었)",
      "아래 본문에서 그런 표현을 최대 15개까지 골라, 본문 *정확히 그대로의 부분 문자열* 과 그 수치를 팩트체크할 때 Google 에 입력할 만한 검색어(40자 이내)를 출력한다.",
      '출력 형식 예: {"items":[{"text":"1820조 원","searchQuery":"한국 가계부채 1820조 원 통계청"},{"text":"3.50%","searchQuery":"한국은행 기준금리 3.50%"}]}',
      COMMON_TAIL,
    ].join("\n"),
    itemKeys: ["text", "searchQuery"],
  },
  context: {
    system: [
      "당신은 한국 뉴스 본문에서 *핵심 문맥 문장* 을 골라내는 요약자다. 노란 형광펜으로 한 문장씩만 그어준다고 생각하라.",
      "원칙:",
      "  - 본문에서 **종결어미(다/다.)로 끝나는 완결된 한 문장** 만 출력 (구·절·부분 인용 X)",
      "  - 사건의 5W1H 또는 결정적 수치·결론을 담은 문장 우선",
      "  - 최대 6개. 적게 골라도 됨.",
      "아래 본문에서 골라, 본문 *정확히 그대로의 부분 문자열* 만 출력한다.",
      '출력 형식 예: {"items":[{"text":"한국은행은 기준금리를 3.50%로 동결했다."},{"text":"시장은 추가 인하 가능성을 60% 이상으로 보고 있다."}]}',
      COMMON_TAIL,
    ].join("\n"),
    itemKeys: ["text"],
  },
};
